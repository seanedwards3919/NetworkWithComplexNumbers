#ifndef MATH_H
#define MATH_H

#include<stdbool.h>

typedef float RealNumber; 
typedef struct complexNumber {
    RealNumber realPart;
    RealNumber imaginaryPart;
} ComplexNumber;

#define gm_add(a, b)  

#endif
